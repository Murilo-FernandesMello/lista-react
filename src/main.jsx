import React from 'react'
import ReactDOM from 'react-dom/client'
import Inicial from './paginas/pagina-inicial'
import '../src/estilos/global.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Inicial/>
   
  </React.StrictMode>,
)
